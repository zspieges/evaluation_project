sandbox:
  target: dev
  outputs:
      dev:
        type: sqlite
        threads: 1
        database: 'database'
        schema: 'main'
        schemas_and_paths:
          main: 'sqlite/sample.db'
        schema_directory: 'sqlite'